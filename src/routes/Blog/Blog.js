import React from 'react';
import styled from 'styled-components';

const Styled = styled.div`
  width: 100%;
  height: 100%;
`;

const Blog = () => (
  <Styled>
    <div>Blog will come here!</div>
  </Styled>
);

export default Blog;
