import React from 'react';
import styled from 'styled-components';

const Styled = styled.div`
  width: 100%;
  height: 100%;
`;

const Home = () => (
  <Styled>
    <div>Hello world!</div>
  </Styled>
);

export default Home;
