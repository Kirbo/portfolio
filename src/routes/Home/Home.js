import React from 'react';
import styled from 'styled-components';

const Styled = styled.div`
  width: 100%;
  height: 100%;
`;

const Home = () => (
  <Styled>
    Hello world!
  </Styled>
);

export default Home;
